# tutorial-for-gRPC

## 0.Preperation
Installation of Golang Plugin
```BASH
$ go get -u github.com/golang/protobuf/{proto,protoc-gen-go}
$ go get -u google.golang.org/grpc
```
Create new root folder for this project
```BASH
$ mkdir $GOPATH/src/grpc
$ mkdir $GOPATH/src/grpc/{clinet, server}
```
